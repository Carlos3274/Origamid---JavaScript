/* const doc = fetch('./doc.txt') */

/* doc.then(response => {
  response.text().then((body) => {
    console.log(body)
  })
 
}) */

/* const doc = fetch('https://viacep.com.br/ws/60415150/json/')

doc.then(response => {
  return response.json()

}).then((json) => {
  console.log(json)

  const conteudo = document.querySelector('p')
  conteudo.innerText = json.bairro
}) */

/* const imagem = fetch('./img.jpg')

imagem.then(r => r.blob())
.then(blob => {
  const blobUrl = URL.createObjectURL(blob)
  console.log(blobUrl)
}) */

